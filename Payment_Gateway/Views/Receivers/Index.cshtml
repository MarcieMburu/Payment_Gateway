@model IEnumerable<Payment_Gateway.Models.Receiver>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Rec_Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Rec_ID_NO)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Rec_Phone)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Rec_Dst_Acc)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Rec_Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Rec_ID_NO)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Rec_Phone)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Rec_Dst_Acc)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
